services:
  genbi-psql:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: genbi_postgres
      POSTGRES_USER: genbi_postgres
      POSTGRES_DB: db_genbi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  adminer:
    image: adminer:latest
    restart: unless-stopped
    ports:
      - "8081:8080"
    depends_on:
      - genbi-psql

  php:
    build:
      context: .
      dockerfile: docker/php-fpm/Dockerfile
    image: genbi-php-fpm
    restart: unless-stopped
    volumes:
      - ./:/var/www/html:delegated
      - ./docker/php.ini:/usr/local/etc/php/conf.d/custom.ini:ro
    depends_on:
      - genbi-psql

  web:
    image: nginx:stable
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php

volumes:
  pgdata:
    name: genbi_pgdata
